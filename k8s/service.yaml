apiVersion: v1
kind: Service
metadata:
  name: pos-backend
  namespace: pos
  labels:
    app: pos-backend
spec:
  selector:
    app: pos-backend
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP